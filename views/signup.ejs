<!DOCTYPE html>
<html lang="en-US">
    <head>
        <title>DON - Signup</title>
        <meta charset="utf-8"/>
        <meta name="viewport" content="width=device-width,initial-scale=1.0"/>
        <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Alegreya+Sans:100|Amatic+SC:700"/>
        <link rel="stylesheet" href="/stylesheets/login.css"/>
        <link rel="stylesheet" href="/stylesheets/layout.css"/>
        <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css">
    </head>
    <body>
        <div class="center">
            <div class="holder">
                    <% if (message.length > 0) { %>
                        <div class="alert alert-danger" id="alertbox"><%= message %></div>
                    <% } %>
                    <form action="/signup" method="post">
                        <div class="form-group">
                            <label>Email</label>
                            <input type="text" class="form-control" name="email">
                        </div>
                
                        <button type="submit" class="btn">Sign up</button>
                    </form>
                    <hr>
                    <div class="back-main">
                        <p style="font-size: .8em">Already have an account? <a href="/login"> Login</a></p>
                    </div>
            </div>
            <div class="to-home">
               <a title="Go home" href="/">☐</a>
            </div>
        </div>
    </body>
</html>